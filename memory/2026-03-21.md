# 2026-03-21 — Session Log

## Summary
Completed Phases 6-9 of MoneyFlow (lovemonself) project. Full backend + frontend implementation.

## Work Done

### Phase 6 — Gateway Core
- **Location:** `backend/api-gateway/`
- Middleware stack: Auth (Firebase), Correlation ID, Rate Limit (10/s per user), CORS
- Routes: chat, transactions, user, health
- Services: classifier (intent), preferences cache (5min TTL), dispatcher
- Reviews: GLM-5 review, spec alignment fixes
- **Commit:** `cb80f79`, `6aa9a83`

### Phase 7 — Gateway Routes
- **Location:** `backend/api-gateway/routes/`
- New routes: budget.py, summaries.py, analytics.py
- Enhanced: transactions.py (list/detail/pagination), user.py (profile/categories)
- **Commit:** `3e837d7`, `81a9208`
- Tests fixed: `f18bb2f` (23 tests passing, call classify_intent() directly)

### Phase 8 — AI Services
- **Locations:** `backend/ask-service/`, `backend/log-text-service/`, `backend/log-image-service/`
- Added `/job` endpoints to all services
- Write results back to Firestore job documents
- Dual-response pattern for ask-service (Claude + Qwen)
- Tests: `test_ask_service.py`, `test_log_text_service.py`
- **Commit:** `db1c3c1`

### Phase 9 — Frontend
- **9.1 lib/api.ts** — API Gateway client (607 lines)
  - All endpoints: chat, transactions, budget, summaries, analytics, user
  - Full TypeScript types for all responses
  - **Commit:** `8c5b97a`

- **9.2 lib/a11y.ts** — Accessibility utilities (395 lines)
  - Focus management, screen reader announcements, keyboard navigation
  - React hooks: useFocusTrap, useAnnounce, useEscapeKey

- **9.3 hooks/useChat.ts** — Chat hook (340 lines)
  - Message state, send ASK/LOG, job polling, confirmation handling

- **9.4 hooks/useBudget.ts** — Budget hook (330 lines)
  - Budget summary, trends, analytics, summaries loading
  - Auto-refresh, helpers (formatMoney, isOverBudget)

- **9.5 dashboard-kit/** — Reusable components (1808 lines)
  - BudgetCard, TransactionList, CategoryChart, TrendChart
  - StatCard, ProgressBar, SummaryCard, QuickActions
  - Full types, utils, CSS styles
  - **Commit:** `56817ef`, `143823a`

## Tickets Created
- S15-create-shared-prompt-utils.md (code_quality)
- S15-add-contracts-tests.md (testing)
- S15-create-frontend-contracts.md (frontend)

## Specs Created
- `docs/specs/GATEWAY_CORE_SPEC.md` — 17KB, 13 sections
- `docs/reviews/gateway-core-review.md` — Spec vs implementation
- `docs/reviews/gateway-core-glm5-review.md` — GLM-5 findings
- `docs/reviews/phase7-review.md` — Phase 7 review

## Key Decisions
- API Gateway as single entry point (not direct service calls)
- Dual-response pattern for ASK mode (Claude + Qwen parallel)
- Token bucket rate limiting (10/s per user)
- Preferences cache with 5-min TTL
- Job polling pattern (client polls Firestore)

## Repos
- lovemonself: `github.com/yi1jack0/lovemonself` — pushed to main
- vtic: `github.com/661818yijack/vtic` — tickets tracking lovemonself work

## Remaining (Technical Debt)
- Token bucket cleanup (memory leak for idle users)
- CSV export streaming limits
- Category name validation
- Correlation ID in logs (needs structlog)
- Test coverage for dashboard-kit components

---

## Frontend Integration (17:00)

### Changes
- **Gateway:** Added legacy endpoints for backward compatibility
  - `POST /api/ask` → wraps `/api/chat` with mode='ask'
  - `POST /api/log/text` → wraps `/api/chat` with mode='log'
  - `POST /api/log/image` → wraps `/api/chat` with mode='log' + image_uri
- **API Client:** Already had all methods (legacy + new)
- **Dashboard:** Uses existing `api.askService()`, `api.extractFromText()`, `api.extractFromImage()` which now route through gateway

### Flow
```
Dashboard → api.askService() → POST /api/ask → Gateway → /api/chat → AI Services
```

### Commit
- `92cbe86` — +151 lines (backend routes/chat.py, frontend lib/api.ts)

### Status
- **ALL PHASES 1-9 + FRONTEND INTEGRATION COMPLETE**
- Backend: 8 phases (Gateway Core, Routes, AI Services)
- Frontend: 5 modules (lib/api, a11y, useChat, useBudget, dashboard-kit)
- Total: ~6000 lines of new code across all phases

---

## Auth Consolidation (23:50)

### Issue
Phase 6 accidentally created duplicate `middleware/auth.py` instead of using existing `shared/auth.py` from Phase 3.

### Fix
- Deleted `backend/api-gateway/middleware/auth.py`
- All routes migrated to `shared.auth.verify_firebase_token`
- Removed `AuthMiddleware` from middleware stack (routes use Depends pattern)
- Updated 6 route files: chat, transactions, user, budget, summaries, analytics

### Commit
- `3181391` — -121 lines (net reduction)

### Result
- **S1 ticket closed** — auth is now consolidated to single module
- `shared/auth.py` is better: 75 tests, 92% coverage, issuer/audience validation, agent auth support

---

## Ticket Review (00:01-00:05)

### S2 - CORS Configuration
**Status:** Already fixed ✅

Gateway reads from `ALLOWED_ORIGINS` env var. AI services use `["*"]` but are internal-only.

### S3 - Route Frontend Through Gateway
**Status:** Not applicable ✅

Next.js frontend was removed in commit `2388a79` (Mar 21, 17:18). Project is now agent-only — agents call API directly via Discord. No browser frontend exists.

---

### S4 - Input Validation (00:08-00:15)
**Status:** Done ✅

**Commit:** `a0eb5b4`

**Changes:**
- Added Pydantic validators to `TransactionConfirmRequest` and `TransactionUpdateRequest`
  - `amount`: must be positive, max RM1M (100000000 cents)
  - `date`: must be YYYY-MM-DD format
  - `category`: must be in whitelist
- Created `TransactionError` class for transaction-internal errors
- Refactored `confirm_txn`, `update_txn`, `delete_txn` in transactions.py to raise `TransactionError` inside `@db.transactional`, catch outside and convert to HTTPException
- Same pattern applied to `sync_txn` in user.py

**Result:** Prevents Firestore SDK retries on validation errors. All 422 responses now come from Pydantic validators before transactions.

---

## Overnight Ticket Work (2026-03-22 00:18)

### S15-create-shared-prompt-utils ✅ — commit `cf8d7fe`
- Created `backend/shared/prompt_utils.py` with `format_money`, `extract_budget_context`, `format_category_breakdown`
- 18 tests in `backend/shared/test_prompt_utils.py` (all pass)
- Refactored `ask-service/prompts.py` to import from shared, eliminating duplicated budget calculation and breakdown logic between `build_claude_prompt` and `build_qwen_prompt`

### S15-add-contracts-tests ✅ — commit `8f64de5`
- Created `backend/shared/test_contracts.py` with 57 tests
- Covers all 4 contracts source files: enums, requests, responses
- Tests: model instantiation, field validation, edge cases (zero/negative amounts, invalid dates, out-of-range values), optional fields

### S13-add-ai-retry-error-handling ✅ — commit `a3bddce`
- Added `_with_retry` helper to ask-service and log-text-service orchestrators: exponential backoff (3 retries, 1/2/4s delays), detects retryable errors from handler return strings (429, 503, rate limit, overloaded)
- Added `asyncio.wait_for(gather(...), timeout=30.0)` with graceful TimeoutError handling
- Added same retry to log-image-service Qwen-VL and Gemini calls (within existing 50s timeout)

### S9-add-agent-auth-rate-limiting ✅ — commit `3ab7cfd`
- Created `backend/api-gateway/services/agent_auth.py` with `verify_agent_token` FastAPI dependency
- Rate limiter now configurable via `RATE_LIMIT_PER_MINUTE` (default 600=10/s) and `RATE_LIMIT_BURST` (default 20)

### S7-add-python-tests-critical-paths ✅ — commit `700743b`
- 73 new tests across 3 files: test_auth.py (20), test_transactions.py (25), shared/tests/test_models.py (28)
- Auth tests mock firebase_admin.auth; all edge cases covered

### S8-add-github-actions-ci-pipeline ✅ — commit `75923cb`
- Added `moneyflow-python-tests` and `moneyflow-rust-tests` jobs to `.github/workflows/ci.yml`

### S10-build-summary-budget-api ✅ — commit `ef6c8b1`
- GET /api/summary?period=month|week (new routes/summary.py)
- GET /api/budget/status[?category=X], POST /api/budget/set, GET /api/budget/alerts (added to budget.py)
- Alert thresholds: 50% info, 75% warning, 90% critical, 100%+ exceeded

### S11-build-transaction-trends-export-api ✅ — commit `75dd841`
- GET /api/trends?months=N (new routes/trends.py): reads monthlySummaries, returns direction (up/down/stable ±5%)
- GET /api/transactions/export?month=YYYY-MM (new routes/export.py): CSV download, Content-Disposition attachment

### S14-add-caching-frontend-polish ✅ — commit `6254e03`
- preferences.py TTL reduced from 300s to 60s (fresher data after budget updates)
- Removed GCS public URL fallback in image_utils.py (security risk)
